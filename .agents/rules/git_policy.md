# Git Remote Policy

- **NEVER** run `git push` or push changes to GitHub or any remote repository.
- Only create local commits or leave working tree edits as requested.
- Remote repository pushes must only be initiated by the user or upon explicit, unambiguous user request.
